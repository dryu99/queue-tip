import React from 'react';
import { Button, Container } from 'react-bootstrap';

const Home = () => {
  return (
    <Container>
      <h1>Home</h1>
      <Button>Create Room</Button>
    </Container>
  );
};

export default Home;